__pycache__/
*.pyc
job_*.log
server.log
.env
node_modules/
.DS_Store

# local-only API keys — never commit
site/keys.js
keys.js

# daily live data (regenerated each run)
systemic_model/live_inputs.json

# Local Netlify folder
.netlify

# Claude Code local settings (personal, never commit)
.claude/settings.local.json
